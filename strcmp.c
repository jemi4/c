#include <stdio.h>
#include <string.h>
int main()
{
   char p[50], j[50];
   printf("Enter a string\n");
   gets(a);
   printf("Enter a string\n");
   gets(b);
 
   if (strcmp(a>b))
      printf("string p is greater");
   else
      printf("string j is greater");
      return 0;
}
