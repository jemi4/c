include <stdio.h>
#include<conio.h>
void main()
{     
      clrscr();
      double ftNum, sdNum, temporaryVariable;
      printf("Enter ft num: ");
      scanf("%lf", &ftNum);
      printf("Enter sd num: ");
      scanf("%lf",&sdNum);
      temporaryVariable = ftNum;
      firstNumber = sdNum;
      sdNum = temporaryVariable);
      printf("\nAfter swapping, ftNum= %.2lf\n", ftNum);
      printf("After swapping, sdNum = %.2lf", sdNum);
      getch();
}
